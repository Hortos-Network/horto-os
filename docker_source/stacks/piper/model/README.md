# Piper model files

GitHub Actions downloads the RK3576 Piper release archive into this directory before building. Do not commit these model assets:

- `encoder.onnx`
- `decoder_rk3576.rknn`
- `config.json`
